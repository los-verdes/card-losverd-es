node_modules/
dist/
.wrangler/
.dev.vars
.dev.vars.*
coverage/
*.log
.DS_Store
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# Local planning/reference docs -- not part of this repo's history
.ai/

# Phase 1.0.1 spike build artifact (see justfile's verify-pkcs7-openssl)
.verify-pkcs7-bundle.mjs

# Claude Code scratch state (background-agent worktrees, etc.) - not part of
# this repo's history.
.claude/
.legacy-import-bundle.mjs
# Apple pass certificate working directory: holds a private key until it is
# stored in 1Password and pushed. Never commit.
.apple-pass-cert/
# Legacy Postgres export + generated import SQL contain member PII -- never commit.
.legacy-export/
# esbuild output for the Google Wallet checker (see the justfile recipe).
.google-wallet-check.mjs
