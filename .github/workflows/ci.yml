name: CI

on:
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: '24'
          cache: 'npm'
      - uses: extractions/setup-just@v4
      - run: npm ci
      - run: just typecheck
      - run: just lint
      - run: just check-wrangler-envs
      - run: just test-coverage
